import json
import os
from sklearn.model_selection import train_test_split

def split_coco_dataset(image_dir, annotations_dir, output_dir, categories, split_ratios=(0.7, 0.2, 0.1)):
    # Splitting logic (see the earlier COCO splitting script)
    pass
